<div class="form-container radius-box glassy-bg small-10 small-centered medium-8 large-6 columns ">
  <div class="ramka">
    <%= form_for(@imagehome) do |f| %>
      <% if @imagehome.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@imagehome.errors.count, "error") %> prohibited this imagehome from being saved:</h2>

          <ul>
          <% @imagehome.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.label :title, "Название" %><br>
        <%= f.text_field :title %>
      </div>

      <div class="mb1">
        <%= f.label :slider, "Изображение" %>
        <%= f.file_field :slider%> 
      </div>



      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
  </div>
</div>    
